<!DOCTYPE html>
<html>
  <head>
    <%- include('../partial/head'); -%>
  </head>
  <body>
    <%- include('../partial/body_header'); -%>
    <div class="container">
      <% if ( ! user.organization ) { %>
        <div class="alert alert-warning"><strong>所属</strong>を入力してください</div>
      <% } %>
      <div class="page-header"><h2><%= title %></h2></div>
      <form action="/user?_method=patch" method="post">
        <div class="form-group">
          <label for="form-name">名前</label>
          <input
            type="text"
            class="form-control"
            id="form-name"
            name="name"
            placeholder="名前"
            autofocus
            value="<%= user.name %>"
            required
          />
        </div>
        <div class="form-group">
          <label for="form-organization">所属</label>
          <input
            type="text"
            class="form-control"
            id="form-organization"
            name="organization"
            placeholder="所属"
            value="<%= user.organization %>"
            required
          />
        </div>
        <div class="form-group">
          <label for="form-email">メールアドレス</label>
          <input
            type="text"
            class="form-control"
            id="form-email"
            name="email"
            placeholder="メールアドレス"
            value="<%= user.email %>"
          />
        </div>
        <div class="form-group">
          <input
            type="checkbox"
            id="form-notify"
            name="notify"
            value="1"
            <%= user.notify ? "checked" : "" %>
          />
          <label for="form-notify">
            コメントに返信があった場合などに通知メールを受け取る
          </small>
        </div>
        <input type="submit" class="btn btn-sm btn-success" value="送信">
      </form>
    </div>
  </body>
</html>
